# Client-side API base URL for local development
REACT_APP_API_URL=http://localhost:5000

# You can also use proxy in package.json during development instead of this.
# If you set this, restart the React dev server after editing.
